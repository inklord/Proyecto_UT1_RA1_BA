<<<<<<< HEAD
name: Deploy Quartz site

on:
  push:
    branches: [ "main" ]
=======
name: Deploy Quartz to GitHub Pages

on:
  push:
    branches: ["main"]
>>>>>>> 7bdfc871baa9bcef1032f7aef3e635b35571e00b
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
<<<<<<< HEAD
      - uses: actions/checkout@v4

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'      # Quartz 4 requiere Node >= 22

      - name: Upgrade npm
        run: npm i -g npm@^10.9.2

      - name: Install deps
        working-directory: site
        run: npm i                # (no usa npm ci)

      - name: Build Quartz
        working-directory: site
        run: npx quartz build
=======
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: 'site/package-lock.json'

      - name: Install dependencies
        working-directory: site
        run: npm ci

      - name: Ensure exec bit for Quartz CLI
        working-directory: site
        run: chmod +x ./quartz/bootstrap-cli.mjs

      - name: Build Quartz (via Node)
        working-directory: site
        run: node ./quartz/bootstrap-cli.mjs build
>>>>>>> 7bdfc871baa9bcef1032f7aef3e635b35571e00b

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site/public

  deploy:
<<<<<<< HEAD
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
=======
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
>>>>>>> 7bdfc871baa9bcef1032f7aef3e635b35571e00b
        uses: actions/deploy-pages@v4
